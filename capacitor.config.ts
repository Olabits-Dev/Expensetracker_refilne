import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.olabits.expensetracker',
  appName: 'Expense Tracker',
  webDir: 'dist'
};

export default config;
